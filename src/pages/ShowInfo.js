import React from "react";

function ShowInfo(usr) {
    const person = usr
    return (
        <>
            <h1>hello</h1>
            {console.log(person)}
        </>
    );
}

export default ShowInfo;